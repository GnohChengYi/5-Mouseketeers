const prettierConfig = require('@garenagamesupport/prettier-config');

module.exports = {
  root: true,
  parser: 'babel-eslint',
  env: {
    browser: true,
  },
  settings: {
    'import/resolver': {
      node: {},
      webpack: 'webpack',
    },
  },
  plugins: ['prettier'],
  extends: ['./airbnb', 'prettier'],
  overrides: [
    {
      files: ['**/*.ts'],
      extends: ['./typescript', 'prettier/@typescript-eslint'],
    },
    {
      files: ['**/*.vue'],
      extends: ['./vue', 'prettier/vue'],
    },
  ],
  rules: {
    'prettier/prettier': ['warn', prettierConfig],
  },
};
